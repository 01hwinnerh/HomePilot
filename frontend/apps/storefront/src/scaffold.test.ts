import { describe, expect, it } from "vitest";

describe("storefront test runner", () => {
  it("is available for later UI behavior tests", () => {
    expect("HomePilot").toBe("HomePilot");
  });
});
